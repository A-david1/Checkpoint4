{% extends 'base.html.twig' %}

{% block title %}Ajouter une lecture - Bateau Livre{% endblock %}

{% block body %}

    <div class="mt-4  pt-3 container card-book" style="width: 22rem;">
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <span class="d-flex justify-content-center">
        <button class="mt-4 mb-4 btn-brown">Ajouter</button>
    </span>
        {{ form_end(form) }}
    </div>
    <div class="container mt-5 d-flex justify-content-center">
        <a href="#" class="btn-brown">Utiliser la recherche</a>
    </div>

{% endblock %}